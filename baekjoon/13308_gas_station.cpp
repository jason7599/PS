#include <iostream>
#include <algorithm>
#include <queue>

const int MAX_CITIES = 2500;

int n_cities;
int gas_prices[MAX_CITIES + 1];



int main()
{
    std::ios::sync_with_stdio(0);
    std::cin.tie(0), std::cout.tie(0);
}