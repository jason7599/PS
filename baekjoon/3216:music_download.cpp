#include <iostream>

using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0), cout.tie(0);

    int np;
    cin >> np;

    int dl_prog = 0;
    int ms_prog = 0;
    int wait = 0;

    while (np--)
    {
        int ms_len;
        int dl_time;

        cin >> ms_len >> dl_time;

        dl_prog += dl_time;

        
    }

    return 0;
}