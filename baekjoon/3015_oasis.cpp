#include <bits/stdc++.h>

int main()
{
    std::ios::sync_with_stdio(0);
    std::cin.tie(0), std::cout.tie(0);

    int n_people;
    std::cin >> n_people;

    std::stack<int> stk;
    int64_t ans = 0;

    while (n_people--)
    {
        int h;
        std::cin >> h;

        
    }
}