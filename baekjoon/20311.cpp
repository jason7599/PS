#include <bits/stdc++.h>

using namespace std;

int n_tubes;
int counts[300'000];
int ans[300'000];

bool solve(int idx) {
    
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    cin >> n_tubes;
    for (int i = 0; i < n_tubes; i++) {
        cin >> counts[i];
    }
}