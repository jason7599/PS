#include <bits/stdc++.h>

using namespace std;

int g_height, g_width;
bool grid[10][10];


int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n_tc;
    cin >> n_tc;
    while (n_tc--) {
        cin >> g_height >> g_width;
        
    }
}