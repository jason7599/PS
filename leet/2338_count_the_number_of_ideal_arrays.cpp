#include <bits/stdc++.h>

using namespace std;

class Solution {
private:
    const int MOD = 1e9 + 7;
public:
    int idealArrays(int len, int maxVal) {
        
    }
};

int main() {
    Solution sol;
    assert(sol.idealArrays(2, 5) == 10);
    assert(sol.idealArrays(5, 3) == 11);
}