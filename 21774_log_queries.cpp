#include <bits/stdc++.h>

using namespace std;

struct DateTime {
    static constexpr char PARSE_DELIMS[] = {'-', '-', ' ', ':', ':', '#'};
    
    tuple<int, int, int, int, int, int> time;
    int log_level;

    DateTime(const string& str) {
        for (int i = 0; i < 7; i++) {
            
        }
    }
};

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
}