#include <iostream>
#include <string>

int get_value(const std::string& str)
{
    
}

int main()
{
    std::ios::sync_with_stdio(0);
    std::cin.tie(0), std::cout.tie(0);

    int n_tc;
    std::cin >> n_tc;
    while (n_tc--)
    {
        std::string lhs, rhs;
        std::cin >> lhs >> rhs;
    }
}