#include <iostream>
#include <string>

using namespace std;

int fileCount;
string wildCard, fileNames[100];

bool test(string fileName) {

    int index = 0;
    while (index < wildCard.length()) {
        if(wildCard[index] == '*') {
            
        }
    }

}

int main() {

    cin >> wildCard >> fileCount;

    for (int i = 0; i < fileCount; i++) {
        cin >> fileNames[i];
    }

}