#include <iostream>
#include <string>

using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);

    int num_words, target_len;
    string target;

    cin >> num_words >> target_len;
    cin >> target;
    for (int i = 0; i < num_words; i++)
    {
        string word;
    }
}