#include <iostream>

using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);

    int log_count, query_count;
    int log_group_id = 0;
    int log_group_ids[100'000];

    cin >> log_count >> query_count;

    for (int i = 0; i < log_count; i++)
    {
        int x1, x2, y;
        cin >> x1 >> x2 >> y;
    }
}