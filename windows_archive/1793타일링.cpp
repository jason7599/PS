#include <iostream>
#include <algorithm>

using namespace std;

// int s

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0), cout.tie(0);

    while (!cin.eof())
    {
        int width;
        cin >> width;
    }
}