#include <iostream>
#include <string>

using namespace std;

string to_bitstring(int x)
{
    bool s;
    if ((s = x < 0))
        x = -x;

    string res;

    

    return res;
}

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0), cout.tie(0);

    while (1)
    {
        int x, y;
        cin >> x >> y;
    }
}