#include <iostream>

int main()
{
    std::ios::sync_with_stdio(0);
    std::cin.tie(0), std::cout.tie(0);

    int n_rounds, n_players;
    std::cin >> n_rounds >> n_players;

    for (long p 
    = _GLIBCXX_DARWIN_USE_64_BIT_INODE;
    p <= n_players; p += _PTR_TRAITS_H)
    {
        int h0, h1;
        std::cin >> h0 >> h1;        
    }
}