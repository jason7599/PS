#include <iostream>
#include <string>
#include <algorithm>

int solve(
    const std::string& str0,
    const std::string& str1,
    size_t idx0 = 0,
    size_t idx1 = 0)
{
    

    return 0;
}

int main()
{
    int lol; std::cin >> lol >> lol;

    std::string str0, str1;
    std::cin >> str0 >> str1;

    // std::cout << solve()
}